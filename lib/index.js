// Generated by CoffeeScript 1.7.1
(function() {
  var EPub, Q, exec, fs, path, uuid;

  exec = require("child_process").exec;

  path = require("path");

  fs = require("fs");

  Q = require("q");

  uuid = function() {
    return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
      var r;
      r = Math.random() * 16 | 0;
      return (c === 'x' ? r : r & 0x3 | 0x8).toString(16);
    });
  };

  EPub = (function() {
    function EPub(options) {
      var self;
      this.options = options;
      if (!options.meta || options.content) {
        return false;
      }
      console.log(options);
      self = this;
      this.generateTempFile(options).then(function() {
        return self.render();
      });
    }

    EPub.prototype.generateTempFile = function() {
      var defer;
      defer = new Q.defer();
      this.uuid = uuid();
      return fs.mkdirSync(path.resolve(__dirname, "../", this.uuid));
    };

    return EPub;

  })();

}).call(this);

//# sourceMappingURL=index.map
